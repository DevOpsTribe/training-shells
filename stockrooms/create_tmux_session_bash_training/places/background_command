echo "[DevOpsTribe Msg] Lanunch sleep 9999 in background"  >> $HOME/congruit.log
[ "$CONGRUIT_TEST" = "true" ] && (sleep 9999 &)

while true; do
    jobs | grep "sleep 9999"
    ret=$?
    
    if [ $ret -eq 0 ]; then
        kill %n
        break
    fi

    sleep 1
done