echo "[DevOpsTRibe Msg] Create the file foobar.txt with permission 775" | write $USER
while true; do 
    ls -l foobar.txt | awk '{ print $1 }' | grep 'rwxrwxr-x' ;
    if [ $? -eq 0 ];
    then
        exit 0
    fi
    sleep 1; 
done