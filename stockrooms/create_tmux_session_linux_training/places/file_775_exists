while true; do 
    echo "Check if foobar.txt exists and has permission 775"
    ls -l foo | awk '{ print $1 }' | grep 'rwxrwxr-x' ;
    if [ $? -eq 0 ];
    then
        exit 0
    fi
    sleep 1; 
done